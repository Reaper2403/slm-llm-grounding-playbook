# Python Starter SDK

This is a deliberately tiny reference implementation.

It shows:

- a shared ledger object
- a compact prompt packet projection
- simple trigger rules
- conservative merge behavior

It is not a full production framework.
